# Spec layer (L2)

`player.md` is the human-owned acceptance language for the current player.

Agents:

- Do not edit these files to make CI green.
- Propose a patch; a person applies it only when the product decision changed.
- Locator / hit-area / helper changes belong in `e2e/` and `*.test.ts` (L1).
