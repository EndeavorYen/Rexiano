## Bug report

> **狀態說明（2026-08-09 更新）**
>
> 此清單為原始回報，多數項目已在後續版本處理。以下只標註**本次已驗證**的項目，
> 其餘維持原狀待逐項確認，避免把未驗證的項目誤標為完成。

1. ~~樂譜模式的 "等待" 不會等，會直接撥完整曲。這是錯誤的。~~
   **已解決。** 早期以「保持隱藏 canvas 掛載」繞過；現已將播放時鐘抽出為
   `engines/transport/TransportClock.ts`，時間推進不再依附 PixiJS ticker，
   純樂譜模式可以真正卸載 canvas 而播放照常。
2. 雙顯示模式，只顯示琴譜，沒有落下音符和鍵盤，這是錯誤的。
3. icon 都要使用 docs\figure\Rexiano_icon.png
4. 點擊歌曲後，直接撥放，不用再點擊播放
5. 類似 synthesia, 一開始就要選擇練習的模式 (等待 / 自由)
6. ~~先把所有試譜彈奏的問題解決掉~~
   **已解決樂譜正確性的根因。** 解析層原本只保留秒數，樂譜層再用單一
   `tempos[0].bpm` 反推 tick，導致任何變速樂曲的音符時值與小節線全錯
   （一段普通的漸慢會產生多餘小節與幽靈十六分音符），變拍則完全不支援。
   現在 `MidiFileParser` 保留 tick / PPQ / 完整 tempo map，`TempoMap` 做精確
   換算，`convertSongToNotation()` 直接吃 tick。回歸測試見
   `src/renderer/src/features/sheetMusic/MidiToNotation.tempo.test.ts`。
7. 彈奏的畫面有些設定太多餘，請設定 "精簡模式" (沒有過多的設定，只有基礎設定) 和 "進階模式" (完整設定)
8. 彈奏結束後，進入統計頁面
9. 設計有用的統計數據，讓練習者可以知道各種指標或注意事項
10. 彈奏結束 --> 給予分數和獎勵 --> 統計頁面
11. scroll bar 都看不到底部顏色，請驗證目前的前端配色和設計
