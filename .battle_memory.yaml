# AGENT MEMORY — 團隊累積的實戰智慧（~30 行上限）
record: { total: 5, red_wins: 0, white_wins: 5, streak: "WHITE x5" }
past_topics: ["State Sync", "UI & IPC Safety", "Sheet Music Rendering", "App.tsx Root Component", "UI/UX Polish & Design System Consistency"]

red_growth:
  learned: "songCardUtils 的 color 函式混用 hardcoded hex + theme token — 同一函式內不一致是最有力的 finding"
  working_on: "五連敗。表面問題白隊秒修 — 需要找到白隊無法一回合修好的架構級問題"
  hard_won_principle: "color-mix(in srgb, ...) 可以讓 hardcoded 色彩變成 theme-aware — 但更好的做法是加 token"

white_growth:
  learned: "text-on-accent 必須統一 — #fff vs var(--color-bg) 的不一致跨 7 個元件散布，快修但容易漏"
  working_on: "i18n key 新增要同步更新 types.ts + en.ts + zh-TW.ts + test mock — 一個都不能漏"
  hard_won_principle: "will-change 只用在即將改變的元素，不要撒在 88 個靜態元素上"

battle_tested_principles:
  - "併發 async 需要 generation counter（S1）"
  - "重複 pattern 先集中再修（S1-S4）"
  - "engine.status + AudioContext.state 同時檢查（S4）"
  - "text-on-accent 必須統一：全部用 #fff，不混 var(--color-bg)（S5）"
  - "hardcoded 色彩用 color-mix 混 --color-text 保證暗色主題對比度（S5）"
  - "ARIA 語義（role/aria-checked/aria-label）不能只加在部分元件（S5）"
  - "will-change 只用在即將改變的元素，靜態 88 鍵全加 = 88 compositor layers（S5）"
  - "新增 i18n key 要同步 types.ts + 所有 locale files + test mocks（S5）"

judge_calibration: "白隊五連勝。紅隊的 finding 品質在提升（S5 R3 的 songCardUtils 很有價值），但白隊修復速度太快。紅隊需要找到修復成本極高的架構問題。"
