# Battle Archive — Rexiano 紅白對抗賽完整戰史

> 每場對抗結束後由裁判 append。完整保留所有細節，供開發者人工審閱。

---

## Session 1 — 2026-03-14 — 主題：State Synchronization & Cross-Store Consistency

### 戰果
- **勝方：WHITE**
- 紅隊提出 10 個 finding（1 critical, 5 major, 4 minor），白隊修復 8 個，deferred 2 個，未解決 0 個
- 裁判發現 1 個新 minor issue（onSettled unguarded throw in guardedResumeAndStart）

### 回合摘要

**Round 1 (Surface):** 4 findings — DRY violation in useMuteController (R1-01), inconsistent miss dedup in WaitMode (R1-02), destructive error handling in song library store (R1-03), stale callbacksRef via useEffect (R1-04). All fixed.

**Round 2 (Structural):** 3 findings — ABA race in isResumePendingRef boolean guard (R2-01 critical), duplicated resume pattern x3 (R2-02), 200-line subscriber SRP violation (R2-03). R2-01/R2-02 fixed with generation counter + helper extraction. R2-03 deferred.

**Round 3 (Excellence):** 3 findings — Resume error leaves broken playback state (R3-01), no numeric validation on localStorage load (R3-02), missing trackIndex in callbacks (R3-03). R3-01/R3-02 fixed. R3-03 deferred.

### Key Code Changes
- `useMuteController.ts`: Extracted `applyMuteState()`, removed useCallback wrappers
- `WaitMode.ts`: Added `_missedMidis` Set for consistent miss deduplication
- `useSongLibraryStore.ts`: Preserve songs on fetch error
- `usePracticeLifecycle.ts`: Replaced boolean `isResumePendingRef` with generation counter `resumeGenRef`; extracted `guardedResumeAndStart()` helper; added `setPlaying(false)` on resume error
- `useSettingsStore.ts`: Added `clampNum()` helper and numeric validation in `loadSavedSettings()`

### Insights
- **[technique]** Generation counter pattern eliminates ABA race in concurrent async guards
- **[technique]** Centralizing repeated async patterns makes fixes multiplicative
- **[pattern]** DRY violations accumulate in React hooks because effects are written in isolation
- **[pattern]** Input validation at setter level but not load level is a defense-in-depth gap
- **[recommendation]** Refactor 200-line playback subscriber (R2-03) in dedicated session
- **[recommendation]** Add trackIndex to WaitModeCallbacks (R3-03) before multi-track support

### Score Breakdown
| Dimension | Red | White |
|-----------|-----|-------|
| Race Condition Safety (w3) | 9 | 9 |
| State Consistency (w2) | 8 | 9 |
| Callback Lifecycle & Memory (w2) | 7 | 8 |
| Code Cohesion (w1) | 7 | 8 |

---

## Session 2 — 2026-03-14 — 主題：UI Component Robustness & IPC Safety

### 戰果
- **勝方：WHITE** (10/10 修復率, 0 deferred)
- 紅隊提出 10 個 finding（4 major, 6 minor），白隊修復 10 個，未解決 0 個
- 裁判發現 1 個新 minor issue（debounce 200ms magic number）

### 回合摘要

**Round 1 (Surface):** 4 findings — duplicated withTimeout with timer leak (R1-01), error rendered N times per category group (R1-02), hardcoded #fff in SettingsPanel (R1-03), volume slider bypasses useMuteController (R1-04). All fixed.

**Round 2 (Structural):** 3 findings — R1-04 fix introduced redundant store updates at 30Hz (R2-01 regression!), handleSelectSong/handleSelectRecent duplication (R2-02), inconsistent auto-dismiss timeout (R2-03). All fixed. R2-02 extracted shared loadSongViaIpc helper.

**Round 3 (Excellence):** 3 findings — localStorage writes at 30Hz during slider drag (R3-01), stale greeting across time-of-day (R3-02), hardcoded boxShadow color (R3-03). All fixed. R3-01 added debounced persistence.

### Key Code Changes
- `SongLibrary.tsx`: Imported withTimeout from recoveryUtils (eliminated timer leak); extracted loadSongViaIpc shared helper; moved error render outside category map; removed stale greeting useMemo
- `SettingsPanel.tsx`: Replaced hardcoded #fff with var(--color-bg); added mute state guard for volume slider; added debounced volume persistence (200ms); replaced inline boxShadow with subtle-shadow class

### Insights
- **[technique]** Catching regressions from previous round's fixes (R2-01) is high-value: fixes introduce new attack surface
- **[pattern]** High-frequency UI events (slider drag) need 3-tier treatment: live audio (instant) + debounced persist (200ms) + guarded state transitions (edge-only)
- **[pattern]** Promise.race timeout pattern leaks timers — always clearTimeout on both resolve and reject
- **[recommendation]** Add accessibility-focused dimension in future battles — lowest scoring area (6-7)

### Score Breakdown
| Dimension | Red | White |
|-----------|-----|-------|
| IPC & Timer Safety (w3) | 8 | 9 |
| UI Consistency & Correctness (w2) | 8 | 9 |
| DRY & Code Reuse (w2) | 8 | 9 |
| Accessibility & UX Resilience (w1) | 6 | 7 |

---

## Session 3 — 2026-03-14 — 主題：Sheet Music Rendering Correctness & Code Quality

### 戰果
- **勝方：WHITE** (10/10 修復率, 0 deferred — 連續第 2 場完美修復)
- 紅隊提出 10 個 finding（4 major, 6 minor），白隊修復 10 個，未解決 0 個

### 回合摘要

**Round 1 (Surface):** 4 findings — dead legato label code (R1-01), duplicated treble/bass stave setup (R1-02), unknown[] type loss (R1-03), void trick for unused function (R1-04). All fixed.

**Round 2 (Structural):** 3 findings — treble/bass note mapping duplication 80 lines (R2-01, the larger cousin of R1-02), density calc allocations (R2-02), hardcoded CSS fallback colors (R2-03). All fixed via buildVoiceNotes helper + countOnsets module-level function.

**Round 3 (Excellence):** 3 findings — staccato tick precision mismatch between two independent fraction calculations (R3-01 — best finding of the session), voice creation duplication (R3-02), inline closure in useMemo (R3-03). All fixed.

### Key Code Changes
- `SheetMusicPanel.tsx`: Extracted setupStave, buildVoiceNotes, createVoice, countOnsets helpers; fixed staccato tick matching to use normalizeTickInMeasure consistently; removed dead code and type casts; used VFStaveNote[] for proper type safety; removed CSS fallback colors

### Insights
- **[technique]** Two independent calculations matching on rounded integers is fragile — always use the same function for both sides (S3 R3-01 staccato matching)
- **[pattern]** DRY violations in render functions accumulate when treble/bass processing mirrors each other — extract "per-clef" helpers early
- **[recommendation]** SheetMusicPanel is still ~950 lines. Consider decomposing into SheetMusicRenderer (VexFlow logic) + SheetMusicLayout (React state) in a dedicated session

### Score Breakdown
| Dimension | Red | White |
|-----------|-----|-------|
| Rendering Correctness (w3) | 8 | 9 |
| Code Structure & DRY (w2) | 9 | 9 |
| Type Safety (w2) | 7 | 9 |
| Performance & Memory (w1) | 7 | 8 |

---

## Session 4 — 2026-03-14 — 主題：App.tsx Root Component — Architecture, Correctness & Hardcoded Values

### 戰果
- **勝方：WHITE** (10/10, 0 deferred — 連續第 3 場完美)
- 紅隊提出 10 個 finding（4 major, 6 minor），白隊修復 10 個

### 回合摘要
**Round 1:** IPC timeout missing (R1-01), hardcoded colors (R1-02), useCallback wrapper (R1-03), getTempoAtTime duplication (R1-04). All fixed.
**Round 2:** Set allocation storm (R2-01), audio explosion on resume failure (R2-02), drag error pattern (R2-03). All fixed.
**Round 3:** Stale AudioContext check (R3-01), practiceCallbacks stability (R3-02), EMPTY_SET constant (R3-03). All fixed.

### Key Insights
- engine.status + AudioContext.state must BOTH be checked for health
- resume failure requires allNotesOff to prevent queued-note explosion
- Transient UI state (hit/miss highlights) needs has() guard to skip redundant Set allocation

---

## Session 5 — 2026-03-14 — 主題：UI/UX Polish & Design System Consistency

### 戰果
- **勝方：WHITE** (13/13 fixed, 0 unresolved — 連續第 5 場完美)
- 紅隊提出 13 個 finding（9 major, 4 minor），白隊修復 13 個，未解決 0 個

### 回合摘要
**Round 1 (Surface):** Hardcoded colors in DeviceSelector (R1-01), DifficultyHeatmap (R1-02), dead-code compact sizes in TransportBar (R1-03), undersized toggle switch (R1-04), inline rgba shadows (R1-05). All fixed.

**Round 2 (Structural):** text-on-accent inconsistency #fff vs var(--color-bg) across 7 components (R2-01), missing ARIA semantics on SongLibraryFilters + ThemePicker (R2-02), PianoKeyboard hardcoded fontFamily (R2-03), ThemePicker non-i18n title (R2-04). All fixed.

**Round 3 (Excellence):** songCardUtils 3 color functions with hardcoded hex + mixed hex/token contract (R3-01), 88 piano keys with willChange compositor layers (R3-02), grade descriptions not i18n-aware (R3-03), black key labels hardcoded rgba (R3-04). All fixed.

### Files Modified (19 files)
- `features/fallingNotes/PianoKeyboard.tsx` — font token, will-change removal, label color
- `features/fallingNotes/TransportBar.tsx` — compact mode sizes
- `features/midiDevice/DeviceSelector.tsx` — error/success color tokens
- `features/practice/DifficultyHeatmap.tsx` — color-mix theme awareness
- `features/practice/PracticeModeSelector.tsx` — themed shadow
- `features/practice/ScoreOverlay.tsx` — CSS shadow class
- `features/practice/SongCompleteOverlay.tsx` — CSS shadow class
- `features/practice/SpeedSlider.tsx` — themed shadow
- `features/settings/SettingsPanel.tsx` — toggle size, text-on-accent consistency
- `features/settings/ThemePicker.tsx` — ARIA, i18n, dot size
- `features/songLibrary/SongCard.tsx` — i18n grade descriptions
- `features/songLibrary/SongLibraryFilters.tsx` — ARIA radiogroup
- `features/songLibrary/songCardUtils.ts` — theme-aware colors, i18n keys
- `i18n/types.ts` — new keys
- `locales/en.ts` — new translations
- `locales/zh-TW.ts` — new translations
- Tests updated: ThemePicker.render.test.tsx, SongCard.test.ts

### Insights
- **[technique]** color-mix(in srgb, #hex NN%, var(--color-text)) makes hardcoded semantic colors theme-adaptive without full tokenization
- **[pattern]** "text-on-accent" inconsistency (#fff vs var(--color-bg)) accumulated because different developers made different choices — needs a documented convention
- **[pattern]** ARIA completeness is uneven — PracticeModeSelector had full ARIA, but sibling components (SongLibraryFilters, ThemePicker) had none
- **[technique]** Removing will-change from static elements (88 piano keys) reduces GPU compositor layer count without affecting visual quality
- **[recommendation]** Add a lint rule or convention: "new i18n key = types.ts + ALL locale files + test mocks"

### Score Breakdown
| Dimension | Red | White |
|-----------|-----|-------|
| Visual Consistency & Token Compliance (w3) | 8 | 10 |
| Interaction Polish & Micro-feedback (w2) | 7 | 9 |
| Child-Friendly UX & Accessibility (w2) | 8 | 10 |
| Layout Robustness & Responsive Behavior (w1) | 7 | 9 |

---

## Session 6 — 2026-03-15 — 主題：Practice Engine Correctness & Cross-Engine Consistency

### 戰果
- **勝方：WHITE** (11/11 fixed, 0 unresolved — 連續第 6 場完美修復)
- 紅隊提出 11 個 finding（8 major, 3 minor），白隊修復 11 個，未解決 0 個
- 紅隊發現了 FreeScorer 引擎完全未整合的系統性架構缺陷 — 歷來最有力的攻擊

### 回合摘要

**Round 1 (Surface):** 4 findings — FreeScorer never reset on mode change (R1-01), MIDI input only routed to WaitMode not FreeScorer (R1-02), FreeScorer.tick() never called from tickerLoop (R1-03), SpeedController lerp formula uses exponential ease not linear (R1-04). All fixed. R1-01/02/03 constitute a single systemic gap: FreeScorer engine existed with passing tests but was never wired into the application lifecycle.

**Round 2 (Structural):** 4 findings — WaitMode/FreeScorer callback DRY violation ~60 lines duplicated (R2-01), loop seek doesn't reset FreeScorer causing notes to never re-score on loop (R2-02), activeTracks change doesn't re-init FreeScorer/StepMode (R2-03), ScoreCalculator engine is dead code never called in production (R2-04). All fixed. R2-01 resolved via shared handleNoteHit/handleNoteMiss helpers. R2-04 resolved by using usePracticeStore.resetScore() as single source of truth.

**Round 3 (Excellence):** 3 findings — FingeringEngine two-pass design overwrites chord fingering with sequential heuristic (R3-01), tickerLoop reads practice mode twice per frame (R3-02), WaitMode exact-match requirement hostile to 6-year-old users (R3-03). All fixed. R3-01 fixed via chordIndices Set to skip chord notes in sequential pass. R3-03 fixed via allowExtraKeys option synced to kidMode setting.

### Files Modified (7 files)
- `engines/fallingNotes/tickerLoop.ts` — FreeScorer.tick() integration, single practiceMode read per frame
- `engines/practice/FingeringEngine.ts` — chordIndices to prevent sequential overwrite of chord fingering
- `engines/practice/SpeedController.ts` — _lerpStartSpeed for true linear interpolation
- `engines/practice/WaitMode.ts` — allowExtraKeys option for kid-friendly input
- `features/practice/usePracticeLifecycle.ts` — Full FreeScorer lifecycle integration (mode change, play/pause, loop seek, activeTracks, MIDI input, callbacks), shared hit/miss handlers, store-based score reset
- `features/practice/usePracticeLifecycle.test.ts` — Updated mocks for freeScorer, stepMode, allowExtraKeys; updated score reset assertions

### Insights
- **[technique]** "Engine exists with tests" does not mean "engine is integrated" — FreeScorer had 13 passing tests but was never called from the application
- **[technique]** Shared callback helpers (handleNoteHit/handleNoteMiss) prevent divergence when multiple engines need identical hit/miss recording, visual feedback, and combo logic
- **[pattern]** New engine integration requires 6 touchpoints: init/dispose, callbacks, mode switch, play/pause, loop seek, activeTracks change — missing any one causes silent failure
- **[pattern]** 60fps hot path should consolidate store reads — reading usePracticeStore.getState() once per frame instead of twice halves overhead and prevents frame-internal consistency issues
- **[recommendation]** Add an integration checklist to CLAUDE.md: when adding a new practice engine, verify all 6 lifecycle touchpoints are wired

### Score Breakdown
| Dimension | Red | White |
|-----------|-----|-------|
| State Machine Correctness (w3) | 8 | 10 |
| Cross-Engine Data Consistency (w3) | 10 | 9 |
| Input Handling Robustness (w2) | 8 | 9 |
| Algorithm Correctness (w2) | 9 | 9 |

---

## Session 7 — 2026-03-15 — 主題：Audio Pipeline Correctness & Resilience

### 戰果
- **勝方：WHITE** (10/10 fixed, 0 unresolved — 連續第 7 場完美修復)
- 紅隊提出 10 個 finding（2 critical, 5 major, 3 minor），白隊修復 10 個，未解決 0 個
- 紅隊首次攻擊修復本身（R3-01 證明 R2-01 fix 不完整）— 歷來最高技巧的攻擊

### 回合摘要

**Round 1 (Surface):** 4 findings — withTimeout() zero test coverage (R1-01), setReleaseTime() + concurrent init/dispose untested (R1-02), _fillGaps() O(n²) algorithm mutating map during iteration (R1-03), delay() utility untested (R1-04). All fixed with comprehensive tests and binary-search optimization.

**Round 2 (Structural):** 3 findings — _resetCursors() skips partially-elapsed notes causing silence on seek into sustained notes (R2-01 critical), ScheduledNote interface dead code (R2-02), clearAudioRecovery() doesn't cancel success timer (R2-03). R2-01 fixed with backwards scan. R2-02 removed. R2-03 fixed with timer cleanup.

**Round 3 (Excellence):** 3 findings — R2-01 backwards-scan fix is INCOMPLETE: stops at first non-matching note, missing earlier long notes behind short notes (R3-01 critical — attacking the fix itself), setSong() doesn't compute maxDuration for bounded scan (R3-02), missing test for short-note-between-long-notes pattern (R3-03). All fixed: maxDuration-bounded scan replaces greedy scan, pre-computed per-track maxDuration, 2 new multi-pattern tests.

### Files Modified (5 files)
- `engines/audio/AudioScheduler.ts` — _findCursorPosition with maxDuration-bounded backwards scan, _maxNoteDurations array, setSong() pre-computation
- `engines/audio/SoundFontLoader.ts` — _fillGaps() replaced with O(G log S) binary search approach
- `engines/audio/types.ts` — Removed dead ScheduledNote interface
- `stores/usePlaybackStore.ts` — clearAudioRecovery() timer cleanup
- Test files: recoveryUtils.test.ts (+7 tests), AudioEngine.test.ts (+9 tests), AudioScheduler.test.ts (+5 tests)

### Insights
- **[technique]** Fix-in-fix attack: proving a repair is incomplete (R3-01 vs R2-01) is the strongest Round 3 strategy — it compounds with the original finding
- **[technique]** Pre-computing per-track maxDuration in setSong() provides a provably correct bound for backwards scan — amortized O(1) per query
- **[technique]** Binary search for _fillGaps nearest-neighbor using sorted directKeys avoids O(n²) degradation from map mutation during iteration
- **[pattern]** Timeline search with variable-length events needs end-time awareness: binary search on start-time alone skips sustained notes after seek
- **[pattern]** Greedy backwards scan (stop at first non-match) fails with non-monotonic end times — real piano music has short passing notes between long sustained notes
- **[pattern]** Test coverage concentrates on core logic but misses utility functions (withTimeout, delay, setReleaseTime) and lifecycle edge cases (concurrent init/dispose)
- **[recommendation]** Add integration test scenarios with realistic multi-duration patterns (bass whole-notes + melody eighth-notes)
- **[recommendation]** Run periodic unused-export audits on engine type files

### Score Breakdown
| Dimension | Red | White |
|-----------|-----|-------|
| Timing & Scheduling Correctness (w3) | 10 | 9 |
| Resource Lifecycle & Leak Prevention (w3) | 7 | 9 |
| Error Recovery Robustness (w2) | 7 | 9 |
| Test Fidelity & Coverage Gaps (w2) | 8 | 10 |

---

## Session 8 — 2026-03-15 — 主題：Zustand Store Layer & Data Persistence Integrity

### 戰果
- **勝方：WHITE** (10/10 fixed, 0 unresolved — 連續第 8 場完美修復)
- 紅隊提出 10 個 finding（1 critical, 8 major, 1 minor），白隊修復 10 個，未解決 0 個
- 紅隊 R2-01 (chain-death) 是歷來最有實際生產影響的 critical finding — Promise chain 一次寫入失敗就永久斷鏈
- 裁判發現 1 個新 minor issue（recentFilesHandlers LOAD 缺少 defensive copy）

### 回合摘要

**Round 1 (Surface):** 4 findings — writeChain silent data loss after failed write (R1-01 major), setDailyGoal no input validation (R1-02 major), duplicate play-session timing in initAutoSave vs App.tsx (R1-03 major), todayKey() uses UTC instead of local timezone (R1-04 minor). All fixed: added in-memory cache, clamping, consolidated timing, local date.

**Round 2 (Structural):** 3 findings — R1-01 fix INCOMPLETE: writeChain.then() rejection kills chain permanently (R2-01 critical fix-in-fix), initAutoSave cleanup doesn't reset _sessionStartTime causing stale timestamps on remount (R2-02 major), mute controller operation ordering creates transient cross-store desync (R2-03 major). All fixed: .catch(() => {}) chain guard, full module-level state cleanup, Read-Mutate-Persist ordering.

**Round 3 (Excellence):** 3 findings — LOAD_SESSIONS returns cache by reference creating dual-truth window (R3-01 major), addPracticeTime has no midnight-boundary detection (R3-02 major), _persistedCache never invalidated by external changes (R3-03 major). All fixed: defensive spread copy, date-staleness detection, storage event listener.

### Files Modified (6 files)
- `src/main/ipc/progressHandlers.ts` — In-memory cachedSessions cache, .catch() chain guard, defensive copy on LOAD, cache reset on registration
- `src/main/ipc/recentFilesHandlers.ts` — In-memory cachedRecents cache, .catch() chain guard, cache reset on registration
- `src/renderer/src/stores/useProgressStore.ts` — Local timezone todayKey(), setDailyGoal clamping, loadDailyGoal clamping, consolidated daily tracking in initAutoSave, _sessionStartTime cleanup, midnight-boundary detection in addPracticeTime
- `src/renderer/src/stores/useSettingsStore.ts` — Storage event listener to invalidate _persistedCache
- `src/renderer/src/hooks/useMuteController.ts` — Operation ordering: Read → Mutate Live → Persist
- `src/renderer/src/App.tsx` — Removed duplicate daily tracking effect (consolidated into initAutoSave)

### Insights Report
```yaml
# INSIGHTS REPORT — SESSION 8
for_developer:
  top_insights:
    - "Promise chains used for serialization MUST have .catch() — a single rejection permanently kills the chain, silently blocking all subsequent operations. This was a production-threatening bug."
    - "Module-level state in store files (_sessionStartTime, _autoSaveUnsub, etc.) needs comprehensive cleanup — the cleanup function must reset ALL module vars, not just call unsubscribe(). R1-03 fix moved timing into module scope, making this MORE critical."
    - "Cross-store mutation ordering matters: Read → Mutate Live → Persist prevents transient desync where subscribers of store A see stale values in store B."
    - "Date-based localStorage caching (daily progress) MUST detect midnight rollover — without explicit date comparison, practice across midnight corrupts both days' data."
  techniques_discovered:
    - by: RED
      technique: "Fix-in-fix chain attack: R1-01 → White adds cache → R2-01 proves chain still breaks → White adds .catch(). Each round deepened the same finding."
      why_it_worked: "The original bug (stale re-reads) and the structural bug (chain death) were intertwined but separable. Fixing one revealed the other."
    - by: WHITE
      technique: "In-memory cache + .catch() chain guard as the canonical persistence serialization pattern"
      why_it_worked: "Cache absorbs write failures (no data loss), .catch() keeps chain alive (no blocked writes). Together they're provably correct."
    - by: RED
      technique: "Midnight boundary attack — finding edge cases where date-based localStorage fails"
      why_it_worked: "Time-based edge cases are invisible in normal testing but affect real users (6yo Rex practicing before bedtime)."
  recurring_patterns:
    - pattern: "Fixes that move logic from local scope (React effect closure) to module scope introduce new lifecycle hazards"
      implication: "Every consolidation that uses module-level state must audit the cleanup path — local scope provides automatic cleanup, module scope doesn't."
    - pattern: "Parallel implementations (progressHandlers + recentFilesHandlers) get fixed inconsistently"
      implication: "When the same pattern appears in multiple files, fixes should be applied to ALL instances simultaneously. Judge found R3-01 applied to progress but not recents."
  recommendations:
    - priority: high
      action: "Add .catch() chain guard to recentFilesHandlers LOAD_RECENT_FILES (return spread copy, matching progressHandlers)"
      rationale: "Judge-found inconsistency — the pattern should be identical across both handlers."
    - priority: medium
      action: "Add a daily-reset check to app startup — detect if todayPracticeMs stored date differs from current date and call resetDailyProgress()"
      rationale: "The midnight fix handles in-flight sessions, but the app startup path still loads stale data if launched the next day without the localStorage date expiring."
```

### Score Breakdown
| Dimension | Red | White |
|-----------|-----|-------|
| Cross-Store Consistency (w3) | 8 | 9 |
| Persistence Correctness (w3) | 9 | 9 |
| Module-Level State Hygiene (w2) | 8 | 9 |
| Type Safety & Defensive Coding (w2) | 7 | 9 |
| Hook Correctness & React Contract Compliance (w2) | 6 | 8 |

---

## Session 9 — 2026-03-15 — 主題：PixiJS Falling Notes Rendering Pipeline — Correctness, Performance & Resource Lifecycle

### 戰果
- **勝方：WHITE** (9/10 fixed, 1 disputed in WHITE's favor, 0 unresolved — 連續第 9 場勝利)
- 紅隊提出 10 個 finding（2 critical, 8 major），白隊修復 9 個，disputed 1 個（裁判判白隊正確），未解決 0 個
- 紅隊首次在 PixiJS rendering pipeline 中發動三回合 fix-in-fix 連鏈攻擊（R1-02→R2-02→R3-01）

### 回合摘要

**Round 1 (Surface):** 4 findings — getVisibleNotes backward scan causes unsorted results corrupting fingering index (R1-01), computeBeatTimesInRange walks from t=0 every frame O(total_beats) (R1-02), _drawHitLine uses uncached getHitLineColor store access (R1-03), theme subscription created without destroyed guard after async init (R1-04). All fixed.

**Round 2 (Structural):** 3 findings — R1-01 fix introduces O(n) indexOf per visible note in hot path (R2-01, fix-in-fix), R1-02 fix Phase 1 processes tempo/time-sig events in separate loops causing wrong beatInMeasure (R2-02 critical, fix-in-fix), _drawBeatGrid dirty-check uses toFixed(4)+template literal string allocation per frame (R2-03). All fixed: precomputed noteIndexMap, interleaved chronological event processing, numeric dirty-check fields.

**Round 3 (Excellence):** 3 findings — R1-02 Phase 1 only skips tempo event segments but skip-beats loop still walks beat-by-beat for single-tempo songs (R3-01 critical, fix-in-fix continuing the chain), Phase 1 segment-skipping floating-point drift (R3-02, DISPUTED — actually incorrect, multiplication is more precise than iteration), update() reads useSettingsStore+getTrackColor every frame bypassing cache pattern (R3-03). R3-01/R3-03 fixed: arithmetic jump O(1) for single-tempo, subscriber+cache for all store reads. R3-02 disputed correctly.

### Files Modified (4 files)
- `src/renderer/src/engines/fallingNotes/NoteRenderer.ts` — fingering index lookup via precomputed Map, computeBeatTimesInRange with interleaved event processing + arithmetic jump, cached hit line/track palette/showFingering, numeric dirty-check, settings subscription lifecycle
- `src/renderer/src/features/fallingNotes/FallingNotesCanvas.tsx` — destroyed guard after noteRenderer creation, destroyed guard in theme callback
- `src/renderer/src/engines/fallingNotes/NoteRenderer.test.ts` — Updated theme mock with note1-note8, added subscribe to settings mock
- `src/renderer/src/features/fallingNotes/FallingNotesCanvas.test.ts` — Updated theme mock with note1-note8, added subscribe to settings mock

### Insights Report
```yaml
# INSIGHTS REPORT — SESSION 9
for_developer:
  top_insights:
    - "Every Zustand store.getState() call in a 60fps render loop should be replaced with subscribe+cache. The pattern: init() subscribes, callback caches to instance field, hot path reads field, destroy() unsubscribes. This eliminates all store overhead from the render path."
    - "Hot-path optimizations MUST be verified against the most common case, not just the complex case. computeBeatTimesInRange was 'optimized' to skip tempo event segments, but 90%+ of MIDI files have a single tempo — the optimization did nothing for them. The arithmetic jump (R3-01) was the correct fix."
    - "Interleaving events chronologically is critical when multiple event types affect shared state. Processing tempo changes and time-signature changes in separate loops caused beatInMeasure corruption when events occurred between each other."
    - "getVisibleNotes() backward scan for sustained notes produces an unsorted result array. Any code that assumes sequential ordering (arithmetic index offset) will silently produce wrong results. Identity-based lookup (Map or indexOf) is the safe approach."
  techniques_discovered:
    - by: RED
      technique: "Three-round fix-in-fix chain: R1-02 (found bug) → R2-02 (proved fix had interleaving bug) → R3-01 (proved optimization was incomplete for common case)"
      why_it_worked: "Each round built on the previous fix's weakness, creating compounding pressure that forced WHITE to deeply understand the algorithm."
    - by: WHITE
      technique: "Adaptive fix improvement: R1-01 used indexOf, R2-01 upgraded to precomputed Map — each round's fix was better than the last"
      why_it_worked: "Rather than defending the initial fix, WHITE acknowledged the weakness and evolved the solution."
    - by: WHITE
      technique: "Subscriber+cache pattern to eliminate all store access from render loop"
      why_it_worked: "Zustand's subscribe() is O(1) on store change, while getState() in a 60fps loop is 60 unnecessary calls/second. Caching converts per-frame cost to per-change cost."
  recurring_patterns:
    - pattern: "Partial caching — fixing one uncached value while leaving similar values uncached"
      implication: "When applying a caching pattern, audit ALL similar call sites in the same file, not just the one that was flagged. R1-03 fixed hitLine but left showFingering and trackColor uncached."
    - pattern: "Optimization that only helps the complex case but not the common case"
      implication: "Performance fixes should be benchmarked against the 90th percentile use case (simple MIDI files), not just the worst case (complex classical pieces)."
  recommendations:
    - priority: high
      action: "Audit all remaining Zustand getState() calls in engines/ directory for per-frame access patterns and convert to subscriber+cache"
      rationale: "Session 9 found 4 such calls in NoteRenderer alone. Other engine files (tickerLoop, AudioScheduler) likely have similar patterns."
    - priority: medium
      action: "Add a test for computeBeatTimesInRange with mixed tempo+time-sig changes to prevent regressions in the interleaving logic"
      rationale: "The interleaving bug (R2-02) had no test coverage and could regress silently."
```

### Score Breakdown
| Dimension | Red | White |
|-----------|-----|-------|
| Rendering Correctness & Visual Fidelity (w3) | 9 | 9 |
| Resource Lifecycle & Memory Safety (w3) | 7 | 9 |
| 60fps Hot Path Performance (w2) | 9 | 9 |
| React-PixiJS Integration Robustness (w2) | 7 | 9 |
